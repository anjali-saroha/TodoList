# Todo Application
This design has been built using React library with Bootstrap. 
All the components are independent. local storage is used to maintain state.
User can add new todo item in a list

## Instructions

`git clone `

`npm install`

`npm run start` & Enjoy the new look in your browser.

## Tools used

* React 16.7 
* Bootstrap 4
